import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  root: {
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
  },
  header: {
    margin: 15,
  },
  member: {
    margin: 10,
  },
}));
