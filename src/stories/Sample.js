import React from 'react'
import { Container } from '@material-ui/core'

export const Sample = () => {
    return (
        <Container maxWidth="md">
            Sample component
        </Container>
    )
}